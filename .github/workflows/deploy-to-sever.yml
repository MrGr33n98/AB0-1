name: Deploy Frontend

on:
  push:
    branches:
      - main
      - master-frontend

jobs:
  deploy-to-server:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Criar .env.production
        run: |
          echo "NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }}" > .env.production
          # adicione outras envs do frontend aqui se precisar
          cat .env.production

      - name: Build Docker image
        run: |
          docker build -t ab0-frontend:${{ github.sha }} .

      - name: Save Docker image as tar
        run: |
          docker save ab0-frontend:${{ github.sha }} -o ab0-frontend.tar
          chmod 644 ab0-frontend.tar

      - name: Copy docker image to droplet
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          source: ab0-frontend.tar
          target: ~/images

      - name: Load and Run docker container
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          script: |
            # Criar rede se não existir
            docker network create ab0-network || true

            # Carregar nova imagem
            docker load -i ~/images/ab0-frontend.tar

            # Remover container antigo
            docker rm -f ab0-frontend || true

            # Rodar container na rede compartilhada
            docker run -d \
              --name ab0-frontend \
              --network ab0-network \
              --env-file .env.production \
              -p 3000:3000 \
              ab0-frontend:${{ github.sha }}
